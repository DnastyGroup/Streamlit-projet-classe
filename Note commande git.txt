Note commande
--Pour verifier les status de mon projet
git status

--pour ajouter mes modifications
git add .

--pour ajouter les commentaire
git commit -m 'Commentaire'

-- pour l'nevoyer sur mon github
git push origin main
